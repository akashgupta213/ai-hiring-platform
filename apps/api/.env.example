# Real secrets live in the ROOT .env file (apps/api reads it from there).
# This file is just a reminder of what apps/api needs.
DATABASE_URL=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
PORT=3001
WEB_ORIGIN=http://localhost:3000
